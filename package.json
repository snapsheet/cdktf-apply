{
  "name": "cdktf-apply",
  "version": "2.0.0",
  "private": true,
  "description": "GitHub Action for CDKTF plan/validate/apply with approval and diff normalization.",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node24 --outdir=dist",
    "clean": "rm -rf dist",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0",
    "chalk": "^5.3.0",
    "diff": "^5.2.0"
  },
  "devDependencies": {
    "@types/diff": "^5.0.6",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.0.6",
    "esbuild": "^0.27.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3"
  }
}
