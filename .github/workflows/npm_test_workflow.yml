name: NPM Test Coverage

on: [push]

jobs:
  npm_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Use Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24.x"

      - run: npm install
      - run: npm run test
